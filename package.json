{
  "name": "playout",
  "version": "1.0.0",
  "description": "A UI layout library for Playdate",
  "scripts": {
    "start": "onchange -ik 'src/**/*' -e 'src/pdxinfo' -- run-s build preview",
    "build": "pdc demo playout.pdx",
    "preview": "open -g playout.pdx"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "onchange": "^7.1.0"
  }
}
